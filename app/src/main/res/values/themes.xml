<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BlinkitClone" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="android:statusBarColor">@color/yellow</item>
    </style>

    <style name="Theme.BlinkitClone" parent="Base.Theme.BlinkitClone" />

    <style name="MySplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/yellow</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/blinkit_splash</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.BlinkitClone</item>
    </style>

</resources>